{
  "name": "semicoduct-presentation",
  "type": "module",
  "version": "1.0.0",
  "description": "Au | MoS₂ Schottky contact — band alignment & I–V (school presentation)",
  "scripts": {
    "dev": "bun x astro dev --host",
    "build": "bun x astro build",
    "preview": "bun x astro preview",
    "check:render": "bun scripts/render-check.js",
    "check:physics": "bun scripts/physics-check.js",
    "check:contact": "bun scripts/validate-contact.js",
    "check:layout": "bun scripts/layout-audit.js",
    "check:layout:1080p": "bun scripts/layout-1080p.js",
    "check:fermi": "bun scripts/fermi-audit.js",
    "check:live": "bun scripts/validate-live.js",
    "check:flow:physics": "bun scripts/flow-physics-check.js",
    "check:flow:sim": "bun scripts/flow-sim-check.js",
    "check:flow:render": "bun scripts/flow-render-check.js",
    "check:flow:e2e": "bun scripts/flow-e2e.js",
    "check": "bun run check:physics && bun run check:render && bun run check:layout && bun run check:layout:1080p && bun run check:contact && bun run check:flow:physics && bun run check:flow:sim && bun run check:flow:render && bun x astro build"
  },
  "dependencies": {
    "astro": "^7.3.2",
    "katex": "^0.18.7",
    "marked": "^18.0.13"
  },
  "devDependencies": {
    "@napi-rs/canvas": "^1.0.9",
    "playwright": "^1.63.0"
  }
}
